{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permission set granted to the main CyberForge window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
