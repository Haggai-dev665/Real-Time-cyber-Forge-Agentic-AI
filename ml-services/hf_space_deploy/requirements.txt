# CyberForge AI - HuggingFace Space
# Combined Gradio UI + FastAPI REST API

# Core ML
scikit-learn>=1.5.0
pandas>=2.2.0
numpy>=2.0.0
joblib>=1.4.0

# Transformers (for phase-3 BERT URL classifier + DGA detector)
torch>=2.2.0
transformers>=4.40.0
tokenizers>=0.19.0

# Gradio UI
gradio>=5.20.0

# FastAPI REST endpoints (for Heroku backend)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# HF Hub (>=0.26 for Inference Providers / OpenAI-compatible router used by DeepSeek)
huggingface_hub>=0.26.0

# Feature Engineering
tldextract>=5.1.0

# Reasoning LLM: DeepSeek via HF Inference Providers (OpenAI-compatible router).
# Called over plain HTTPS (requests) — no extra SDK needed. Gemini retired.

# Notebook execution
nbformat>=5.10.0
nbconvert>=7.16.0
nest_asyncio>=1.6.0

# HTTP client for notebooks
httpx>=0.27.0

# Data handling
datasets>=2.20.0
pyarrow>=17.0.0

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0
